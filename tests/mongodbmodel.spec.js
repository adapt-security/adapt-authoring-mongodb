const should = require('should');

describe('MongoDBModel', function() {
  describe('#SchemaTypes()', function() {
    it('should return an object', function() {
      false.should.be.true();
    });
  });
  describe('#constructor()', function() {
    it('should expose Mongoose.Schema#plugin()', function() {
      false.should.be.true();
    });
    it('should expose Mongoose.Schema#.add()', function() {
      false.should.be.true();
    });
    it('should expose Mongoose.Schema#pre()', function() {
      false.should.be.true();
    });
    it('should expose Mongoose.Schema#post()', function() {
      false.should.be.true();
    });
  });
  describe('#compile()', function() {
    it('should cache the compiled model', function() {
      false.should.be.true();
    });
  });
});
